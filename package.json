{
  "name": "ficsit-together",
  "private": true,
  "workspaces": [
    "ficlib",
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build -ws",
    "build:lib": "npm run build --workspace ficlib"
  }
}
